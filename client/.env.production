# When deployed on Vercel, the frontend and backend are on the same domain.
# All axios calls use relative /api/... paths, so no base URL is needed here.
# The VITE_API_URL is kept only for reference - it is NOT used in the code.
VITE_API_URL=
